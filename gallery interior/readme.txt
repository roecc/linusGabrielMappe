fuer anwendung auf normalem monitor:

wasd bewegung
maus umsehen
linksclick (halten) text platzieren
links rechts text rotieren
rechtsclick maus freischalten
    -> linksclick auf tafel um darauf zu schreiben
    -> publish button clicken um einen vorhang zu generieren



in vr:
linger trigger text platzieren
rechter trigger zu roter box teleportieren
linker stick text rotieren
linker button keyboard ein/ausschalten
    -> ret um den vorhang zu generieren